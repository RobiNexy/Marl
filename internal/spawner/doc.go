// Package spawner 实现意图裁决关口与进程表（Part 8.1 / 9，13.7 阶段 5）。
//
// 核心原则（原则 1）：任何 Agent（含根）只能**请求** fork；真正的创建由
// Spawner（框架级单例，不属于任何 Agent）统一裁决和执行：
//
//	SpawnRequest → 校验（拓扑/权限/命名空间子集/扇出闸）→ 批准并创建
//	                                          → 拒绝（错误码 + 可读原因）
//
// Spawner 不理解任务语义——它只做拓扑与权限的守门。子 Agent 的构建由
// ChildFactory（装配层注入）完成，Spawner 负责生命周期登记与"框架代报"。
//
// 阶段 5 边界（13.7 "不做"清单）：多个子并行只做机制不做专项测试
// （等待逻辑天然支持 N 个子）、深度 >1（maxDepth 配置可设但测试只测单层）、
// 子崩溃恢复（退出未 report 的子由框架代报 failed，不重启）。
package spawner
